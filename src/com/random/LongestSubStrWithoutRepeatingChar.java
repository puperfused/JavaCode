package com.random;

import java.util.Scanner;

public class LongestSubStrWithoutRepeatingChar {

    public static int solution(String s) {
        int hash[] = new int[26];
       // int len = Integer.parseInt(s.charA);
        return 1;
    }

    public static void main(String args[]) {
        Scanner scan = new Scanner(System.in);
        String s = scan.next();
        int len = solution(s);
        System.out.println("Longest SubString without repeating character is " + len);
    }
}
